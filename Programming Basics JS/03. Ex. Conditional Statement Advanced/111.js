function skiTrip(input) {
    let numDays = Number(input[0]);
    let numNights = numDays - 1;
    let typeRoom = input[1];
    let comment = input[2];
    if (numDays < 10)
    {
        console.log("bau")
    }
}
    skiTrip(["5", "apartment", "positive"])